# TypeSafe console key from https://console.typesafe.ai/keys
# Export this in your shell, or copy this file to .env (gitignored).
# Leave the value empty in git. Never commit a real key.
TYPESAFE_API_KEY=

# Optional. Official SDK defaults:
# TYPESAFE_BASE_URL=https://api.typesafe.ai
# TYPESAFE_DEFAULT_MODEL=jev-latest
# TYPESAFE_LOG_LEVEL=warn
